{% extends "::layout.html.twig" %}

{% block title %} {{ parent() }} - Contacts {% endblock %}

{% block body %}

    <a class="link-inverse" href="{{path('apa_message_inbox')}}"><< Voir les messages reçus</a>

    <h2 class="text-center"> Liste des membres du groupe </h2>


    <table class="table table-hover table-sm">
        <thead>
            <th></th>
            <th></th>
        </thead>
        <tbody>
            {% for user in listUsers %}

                {% if user.id != currentUser.id %}
                <tr>

                    <td><strong>{{user.nom}} {{user.prenom}}</strong></td>

                    <td class="btn-group-vertical btn-group-sm text-right">
                        <a class="btn btn-sm btn-primary"
                           href="{{path('apa_message_sendMessage', {'id':user.id})}}">
                            Ouvrir une conversation
                        </a>
                    </td>

                <tr>
                {% endif %}

            {% endfor %}
        </tbody>
    </table>

    <style type="text/CSS">

        .col-md-2 {
        height: 601px;
          }

          .btn-group-vertical {

          float:right;

          }

    </style>

{% endblock %}
